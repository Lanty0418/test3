GOOGLE_GENAI_USE_VERTEXAI=FALSE
GOOGLE_API_KEY=PASTE_YOUR_ACTUAL_API_KEY_HERE

# Optional: set the Google Cloud project ID to override the default project associated
# with the credentials. If you need to switch to a different project (for example
# to use user B's project), set it here:
# GCP_PROJECT=agent-judge-470913

# Optional: point to a JSON service account key file for the target project. If
# using ADC (Application Default Credentials) you can also run:
#   gcloud auth application-default login --project=agent-judge-470913
# or set the path below to the service account key for user B's project:
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/agent-judge-470913-service-account.json